# Quickstart

    python -m venv .venv
    pip install -r requirements.txt
    python src/generate_data.py
    python src/train.py
    streamlit run app.py

The included dataset is synthetic so the project is reproducible without exposing private customer information.
